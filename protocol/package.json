{
  "name": "@speaki-e/protocol",
  "version": "0.5.0",
  "description": "Speaki-e desktop pet + agent system: shared socket/tool/avatar contract for pet-app, workspace, and ai-module.",
  "license": "UNLICENSED",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist",
    "docs",
    "swift"
  ],
  "repository": {
    "type": "git",
    "url": "https://github.com/Speaki-e/protocol.git"
  },
  "scripts": {
    "prepare": "tsc -p .",
    "build": "tsc -p .",
    "test": "tsc -p tsconfig.test.json && find dist-test -name '*.test.js' -print0 | xargs -0 node --test"
  },
  "devDependencies": {
    "@types/node": "^24.13.3",
    "typescript": "^5.9.3"
  }
}
