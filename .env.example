# Development-only. Production obtains secrets through Electron safeStorage integration.
ANTHROPIC_API_KEY=
# Optional development-only loopback Anthropic-compatible endpoint.
ANTHROPIC_BASE_URL=

# Optional: force the deterministic mock runtime for local integration tests.
WORKSPACE_MOCK_AI=0
